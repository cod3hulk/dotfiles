#!/usr/bin/env bash
# Glyphs come from Hack Nerd Font, which is already installed via
# brew/Brewfile.common (cask "font-hack-nerd-font"). Deliberately avoiding
# sketchybar-app-font so the bar adds no new font dependency.

export ICON_FONT="Hack Nerd Font"

# Battery (nf-md-battery ramp)
export BATT_100=󰁹
export BATT_75=󰂀
export BATT_50=󰁾
export BATT_25=󰁻
export BATT_0=󰂎
export BATT_CHARGING=󰂄

export ICON_CLOCK=
export ICON_CALENDAR=
